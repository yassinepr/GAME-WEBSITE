
<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Jeux Portal</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<header>
  <div class="brand"><a href="/">🎮 Jeux Portal</a></div>
  <form class="searchbar" method="get" action="/">
    <input name="q" placeholder="Rechercher un jeu..." value="<%= q %>">
    <select name="cat">
      <option value="">Toutes catégories</option>
      <% categories.forEach(c => { %>
        <option value="<%= c %>" <%= cat===c?'selected':'' %>><%= c %></option>
      <% }) %>
    </select>
    <button class="primary">Chercher</button>
  </form>
  <div class="row">
    <% if (user==='admin') { %>
      <a class="pill" href="/admin">Admin</a>
      <form method="post" action="/admin/logout"><button>Se déconnecter</button></form>
    <% } else { %>
      <a class="pill" href="/admin/login">Admin</a>
    <% } %>
  </div>
</header>

<div class="container">
  <% if (games.length===0) { %>
    <div class="empty">Aucun jeu ne correspond à votre recherche.</div>
  <% } %>
  <div class="grid">
  <% games.forEach(g => { %>
    <a class="card" href="/game/<%= g.slug %>">
      <img src="<%= g.thumbnail %>" alt="<%= g.title %>">
      <div class="p"><%= g.title %></div>
    </a>
  <% }) %>
  </div>
</div>

<div class="footer">© <span id="y"></span> Jeux Portal — Fait avec Express + EJS</div>
<script>document.getElementById('y').textContent=new Date().getFullYear()</script>
</body>
</html>
